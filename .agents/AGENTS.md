# Project Rules - websiteManicure

## Git Commit & Push Workflow
- Always perform `git add`, `git commit` with a concise descriptive commit message, and `git push origin main` after implementing and verifying changes requested by the user.
